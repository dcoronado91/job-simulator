# Database connection settings
DB_HOST=localhost        # Hostname of the PostgreSQL server (use service name in Docker)
DB_PORT=5432             # PostgreSQL default port
DB_NAME=jobsimulator     # Name of the database
DB_USER=user             # Database user
DB_PASSWORD=password     # Database password

# Application settings
APP_PORT=3000            # Port the Node.js server will listen on
